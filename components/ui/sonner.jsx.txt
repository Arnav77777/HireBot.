"use client";

import { useTheme } from "next-themes";

const Toaster = (props) => {
  const { theme = "system" } = useTheme();

  return (
    <div>
      {/* Replace this with your actual Toaster implementation */}
      <p>Toaster Component</p>
    </div>
  );
};

export { Toaster };
